{
    // Usa uma variável do VS Code para garantir que o caminho está correto
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/Scripts/python.exe",

    // Garante que novos terminais ativem automaticamente o ambiente
    "python.terminal.activateEnvironment": true
}

